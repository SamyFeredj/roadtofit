<%= turbo_stream.append "comments_#{post.id}", target: "comment_list" do %>
  <div id="comment_<%= commentaire.id %>">
    <%= render partial: "commentaires/commentaire", commentaire: commentaire, post: post %>
  </div>
<% end %>
