<div class="posts-container">
  <h1 id="whassup">Quoi de neuf ?</h1>
  <%= render "new_post", post: @post %>
  <% @posts.reverse.each do |post| %>
    <%= render "post", post: post %>
  <% end %>
</div>
